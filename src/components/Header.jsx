import React from 'react'; // Ensure React is imported for JSX

const Header = () => {
  return (
    <header className="app-header">
      <h1>ProjectSim Free</h1>
    </header>
  );
};

export default Header; // Export the component so it can be imported in other files
